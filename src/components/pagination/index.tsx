// import styles from './pagination.module.css'

// type PaginationProps = {
//    limit: number;
//    offset: number;
//    total: number;
// }

// const PAGE = 1;
// const MAX_ITEMS = 9;
// const MAX_LEFT = (MAX_ITEMS - PAGE) / 2;

// export default function Pagination({ limit, offset, total }: PaginationProps) {

//    const currentPage = offset ? offset/limit + PAGE : PAGE;
//    const totalPages = Math.ceil(offset/limit);

//    return (
//       <div>Paginação</div>
//    )
// }
